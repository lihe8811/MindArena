# Client Tests

Use this folder for tests covering `src/client/` behavior:

- page-level state and rendering
- reusable client components
- client API wrapper behavior
- loading, empty, error, disabled, and auth-dependent UI states
